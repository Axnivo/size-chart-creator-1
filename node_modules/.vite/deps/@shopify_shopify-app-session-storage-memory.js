import {
  __publicField
} from "./chunk-VRMXEQCD.js";

// node_modules/@shopify/shopify-app-session-storage-memory/dist/esm/memory.mjs
var MemorySessionStorage = class {
  constructor() {
    __publicField(this, "sessions", {});
  }
  async storeSession(session) {
    this.sessions[session.id] = session;
    return true;
  }
  async loadSession(id) {
    return this.sessions[id] || void 0;
  }
  async deleteSession(id) {
    if (this.sessions[id]) {
      delete this.sessions[id];
    }
    return true;
  }
  async deleteSessions(ids) {
    ids.forEach((id) => delete this.sessions[id]);
    return true;
  }
  async findSessionsByShop(shop) {
    const results = Object.values(this.sessions).filter((session) => session.shop === shop);
    return results;
  }
};
export {
  MemorySessionStorage
};
//# sourceMappingURL=@shopify_shopify-app-session-storage-memory.js.map
